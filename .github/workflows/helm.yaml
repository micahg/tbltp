name: Helm Deploy
run-name: Helm deploy to ${{ inputs.environment }}
on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      extra-helm-args:
        required: false
        type: string
      ref:
        required: true
        type: string
      api-version:
        required: true
        type: string
      mui-version:
        required: true
        type: string
    secrets:
      KUBE_CONFIG:
        required: true
      MONGODB_CONNECTION_STRING:
        required: false
      OTEL_INSTANCE_ID:
        required: false
      OTEL_TOKEN:
        required: false
      LOKI_USER_ID:
        required: false
      LOKI_TOKEN:
        required: false
      STORAGE_S3_ENDPOINT:
        required: true
      STORAGE_S3_BUCKET:
        required: true
      STORAGE_S3_ACCESS_KEY_ID:
        required: true
      STORAGE_S3_SECRET_ACCESS_KEY:
        required: true
jobs:
  helm:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      contents: read
    steps:
      - name: Checkout ${{ inputs.ref }}
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
      - id: preflight
        name: Preflight Checks
        run: |
          ls -l /usr/local/bin
          which helm
          which kubectl
      - id: kubeconfig
        name: Kubectl Config
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        run: |
          if ! [ -d ~/.kube ]; then mkdir ~/.kube; fi
          if ! [ -f ~/.kube/config ]; then touch ~/.kube/config; fi
          chmod go-rwx ~/.kube/config
          echo $KUBE_CONFIG | base64 -d > ~/.kube/config
      - id: clusterinfo
        name: Cluster Info
        run: |
          kubectl cluster-info
      - id: helm
        name: Helm Upgrade
        env:
          ENVIRONMENT: ${{ inputs.environment }}
          DEPLOY_ENV: ${{ vars.DEPLOYMENT_ENVIRONMENT }}
          EXTRA_ARGS: ${{ inputs.extra-helm-args }}
          API_VERSION: ${{ inputs.api-version }}
          UI_VERSION: ${{ inputs.mui-version }}
          RELEASE_VERSION: ${{ inputs.ref }}
          MONGODB_CONNECTION_STRING: ${{ secrets.MONGODB_CONNECTION_STRING }}
          OTEL_INSTANCE_ID: ${{ secrets.OTEL_INSTANCE_ID }}
          OTEL_TOKEN: ${{ secrets.OTEL_TOKEN }}
          LOKI_USER_ID: ${{ secrets.LOKI_USER_ID }}
          LOKI_TOKEN: ${{ secrets.LOKI_TOKEN }}
          STORAGE_S3_ENDPOINT: ${{ secrets.STORAGE_S3_ENDPOINT }}
          STORAGE_S3_BUCKET: ${{ secrets.STORAGE_S3_BUCKET }}
          STORAGE_S3_ACCESS_KEY_ID: ${{ secrets.STORAGE_S3_ACCESS_KEY_ID }}
          STORAGE_S3_SECRET_ACCESS_KEY: ${{ secrets.STORAGE_S3_SECRET_ACCESS_KEY }}
        run: |
          [[ ! -z "$MONGODB_CONNECTION_STRING" ]] && VALUE=`echo "$MONGODB_CONNECTION_STRING"` yq e -i ".mongoConnectionString=env(VALUE)" chart/values.yaml
          [[ ! -z "$OTEL_INSTANCE_ID" && ! -z "$OTEL_TOKEN" ]] && VALUE="Authorization=Basic $(printf '%s:%s' "$OTEL_INSTANCE_ID" "$OTEL_TOKEN" | base64 -w0)" yq e -i ".otelHeaders=env(VALUE)" chart/values.yaml
          [[ ! -z "$LOKI_USER_ID" ]] && VALUE=`echo "$LOKI_USER_ID"` yq e -i ".lokiUserId=env(VALUE)" chart/values.yaml
          [[ ! -z "$LOKI_TOKEN" ]] && VALUE=`echo "$LOKI_TOKEN"` yq e -i ".lokiToken=env(VALUE)" chart/values.yaml
          [[ ! -z "$STORAGE_S3_ENDPOINT" ]] && VALUE=`echo "$STORAGE_S3_ENDPOINT"` yq e -i ".storageS3Endpoint=env(VALUE)" chart/values.yaml
          [[ ! -z "$STORAGE_S3_BUCKET" ]] && VALUE=`echo "$STORAGE_S3_BUCKET"` yq e -i ".storageS3Bucket=env(VALUE)" chart/values.yaml
          [[ ! -z "$STORAGE_S3_ACCESS_KEY_ID" ]] && VALUE=`echo "$STORAGE_S3_ACCESS_KEY_ID"` yq e -i ".storageS3AccessKeyId=env(VALUE)" chart/values.yaml
          [[ ! -z "$STORAGE_S3_SECRET_ACCESS_KEY" ]] && VALUE=`echo "$STORAGE_S3_SECRET_ACCESS_KEY"` yq e -i ".storageS3SecretAccessKey=env(VALUE)" chart/values.yaml
          helm upgrade --install -f chart/values.yaml $EXTRA_ARGS \
            --set apiImageVers="$API_VERSION" \
            --set uiImageVers="$UI_VERSION" \
            --set releaseVersion="${RELEASE_VERSION#v}" \
            --set deployEnv="$DEPLOY_ENV" ntt-infra chart