import { transitionStateMachine } from './statemachine';

describe('State Machine', () => {
  // it('Should handle a perfect fit', () => {
  //   transition('hi');
  // });
  // it('Should should transition with a good case', () => {
  //   let sm = createStateMachine();
  //   let res = transitionStateMachine(sm, 'down', null);
  //   expect(res).toEqual(true);
  //   expect(sm.current).toEqual('record')
  // });
  // it('Should should not transition with a bad case', () => {
  //   let sm = createStateMachine();
  //   let res = transitionStateMachine(sm, 'nwod', null);
  //   expect(res).toEqual(false);
  //   expect(sm.current).toEqual('wait')
  // });
});